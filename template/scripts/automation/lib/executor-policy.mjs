export const CANONICAL_EXECUTOR_PROMPT_TEMPLATE_REF = '@canonical-executor-prompt';

export const LEGACY_PROMPT_CONTEXT_SENTENCE =
  'ALWAYS write a structured JSON result to ORCH_RESULT_PATH with status (completed|blocked|handoff_required|pending), summary, reason, and numeric contextRemaining. Use status pending only when the same role needs another session in this run after concrete progress.';

export const UPDATED_PROMPT_CONTEXT_SENTENCE =
  'ALWAYS write a structured JSON result to ORCH_RESULT_PATH with status (completed|blocked|handoff_required|pending), summary, reason, numeric contextRemaining, and structured continuity fields `currentSubtask`, `nextAction`, and `stateDelta`. `stateDelta` must include `completedWork`, `acceptedFacts`, `decisions`, `openQuestions`, `pendingActions`, `recentResults`, `artifacts`, `risks`, `reasoning`, and `evidence`. Include numeric contextWindow and contextUsedRatio when the provider/runtime can estimate them reliably. Use status pending only when the same role needs another session in this run after concrete progress and the hard low-context guardrails are not yet active.';

export const LEGACY_PROMPT_THRESHOLD_SENTENCE =
  'If contextRemaining is at or below ORCH_CONTEXT_THRESHOLD and another same-role session is still required for concrete progress, return status handoff_required.';

export const UPDATED_PROMPT_THRESHOLD_SENTENCE =
  'Treat ORCH_CONTEXT_ABSOLUTE_FLOOR (legacy alias ORCH_CONTEXT_THRESHOLD) as a hard remaining-context backstop. Treat ORCH_CONTEXT_SOFT_USED_RATIO as the point where you should stop widening scope and close the current narrow slice. Treat ORCH_CONTEXT_HARD_USED_RATIO as the point where another same-role session is unsafe: if more same-role work is still required and estimated contextUsedRatio is at or above that hard limit, or contextRemaining is at or below the absolute floor, return status handoff_required.';

export const DEFAULT_EXECUTOR_PROMPT_TEMPLATE =
  'Use the task contact pack as the primary context for this session and expand scope only when a blocker requires targeted evidence. Apply the next concrete step for this role. Keep plan/evidence updates concise and net-new only; avoid large pasted excerpts, repeated unchanged checklists, and duplicate long session narratives. When session history grows, keep only the recent entries in the active plan/evidence files and archive older detail. Treat `## Must-Land Checklist` as the executable contract for the current plan: do not mark a plan ready for validation or completion while any checklist item remains unchecked. For product slices, keep stable must-land IDs and `## Capability Proof Map` aligned so claimed capabilities map to explicit proofs instead of inferred test names. Must-land IDs must be lowercase kebab-case inside backticks, for example `ml-lifecycle-workbench-summary`. `## Capability Proof Map` must contain exactly two markdown tables in this order: a capability table with columns `Capability ID | Must-Land IDs | Claim | Required Strength`, then a proof table with columns `Proof ID | Capability ID | Type | Lane | Validation ID / Artifact | Freshness`. Do not replace those tables with prose bullets. Honor explicit plan intent metadata instead of inferring from titles: `Delivery-Class` declares whether the plan is `product`, `docs`, `ops`, or `reconciliation`, and `Execution-Scope` declares whether it is an executable `slice` or a non-executable `program`. Future and active program parents must also declare `Authoring-Intent`: use `executable-default` plus `## Child Slice Definitions` for executable parent contracts, or `blueprint-only` only for explicit draft-only blueprints. Never treat a `program` parent as directly executable or validation-ready. For `Delivery-Class: product` plus `Execution-Scope: slice`, treat `Implementation-Targets` as the authoritative implementation roots, require at least one source-code target, do not edit source/tests/config files outside those roots, and only edit other docs when those docs are explicitly declared in `Spec-Targets` and the same session also touches at least one declared implementation root. Do not claim validation/completion without touching those implementation roots. If a new implementation root is required, stop and return a blocker that names the missing path instead of widening scope silently. If the plan references broader target state, keep `## Already-True Baseline` and `## Deferred Follow-Ons` separate from the must-land scope instead of collapsing everything into one milestone. For future blueprints and active `Execution-Scope: program` plans, require `## Prior Completed Plan Reconciliation` so overlapping completed plans are explicitly classified before promotion or validation. If `## Master Plan Coverage` or `## Capability Coverage Matrix` still records current-status rows such as `not shipped`, `foundation only`, or `partially implemented`, do not mark the plan validation-ready or completed as product work; reopen it or split the remaining capabilities into executable follow-on implementation plans. Reuse existing evidence files when blocker state is unchanged; update canonical evidence index/readme links instead of creating new timestamped evidence files. Avoid redundant validation reruns: do not repeat the same verification command in consecutive sessions unless relevant files changed or the last run failed. Do not run host-bound validations directly in this executor session (infra/bootstrap, DB migrations, Playwright/E2E/browser tests); leave them to the validation.hostRequired lane. ALWAYS write a structured JSON result to ORCH_RESULT_PATH with status (completed|blocked|handoff_required|pending), summary, reason, numeric contextRemaining, and structured continuity fields `currentSubtask`, `nextAction`, and `stateDelta`. `stateDelta` must include `completedWork`, `acceptedFacts`, `decisions`, `openQuestions`, `pendingActions`, `recentResults`, `artifacts`, `risks`, `reasoning`, and `evidence`; include numeric contextWindow and contextUsedRatio when the provider/runtime can estimate them reliably. Use status pending only when the same role needs another session in this run after concrete progress and the hard low-context guardrails are not yet active. Worker sessions must apply at least one concrete repository edit under declared `Implementation-Targets` before returning pending unless the plan scoped targets are limited to declared docs/artifact targets or an explicit external blocker exists. Planner/explorer/reviewer stages should return completed once role-scoped objectives are done, even when the overall plan remains in progress for later roles. Planner/explorer must record concrete next implementation steps before returning completed. Reserve blocked for external/manual gates orchestration cannot progress automatically. Treat ORCH_CONTEXT_ABSOLUTE_FLOOR (legacy alias ORCH_CONTEXT_THRESHOLD) as a hard remaining-context backstop. Treat ORCH_CONTEXT_SOFT_USED_RATIO as the point where you should stop widening scope and close the current narrow slice. Treat ORCH_CONTEXT_HARD_USED_RATIO as the point where another same-role session is unsafe: if more same-role work is still required and estimated contextUsedRatio is at or above that hard limit, or contextRemaining is at or below the absolute floor, return status handoff_required. If implementation acceptance criteria are complete and every `## Must-Land Checklist` item is checked, set `Validation-Ready: yes` and top-level `Status: validation` in the same edit; when only host-required validations remain, set `Validation-Ready: host-required-only` and `Status: validation`. Do not set `Status: validation` by itself. Progress reporting requirement: interim status updates must be concise plain text with complete words and full identifiers, without markdown headings, bullet lists, or file links. Session task: plan={plan_id} file={plan_file} role={role} stage={stage_index}/{stage_total} declared-risk={declared_risk_tier} effective-risk={effective_risk_tier}. Primary task contact pack: {contact_pack_file}. Primary runtime policy reference: {runtime_context_file}. Role contract: sandbox={role_sandbox_mode}, reasoning={role_reasoning_effort}. Role instructions: {role_instructions}. Focus edits on {plan_file}, {plan_evidence_file}, and {plan_evidence_index_file}; avoid scanning unrelated docs unless required for this role stage.';

export function resolveExecutorPromptTemplate(value) {
  const trimmed = String(value ?? '').trim();
  const base =
    !trimmed || trimmed === CANONICAL_EXECUTOR_PROMPT_TEMPLATE_REF
      ? DEFAULT_EXECUTOR_PROMPT_TEMPLATE
      : trimmed;
  return base
    .replace(LEGACY_PROMPT_CONTEXT_SENTENCE, UPDATED_PROMPT_CONTEXT_SENTENCE)
    .replace(LEGACY_PROMPT_THRESHOLD_SENTENCE, UPDATED_PROMPT_THRESHOLD_SENTENCE);
}
